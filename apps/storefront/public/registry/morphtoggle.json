{
  "name": "morphtoggle",
  "title": "Morph Toggle",
  "category": "input",
  "description": "A premium, animated Morph Toggle component built with React, Framer Motion, and Tailwind CSS.",
  "isPro": false,
  "dialects": {
    "tsx": {
      "language": "tsx",
      "files": [
        {
          "name": "MorphToggle.tsx",
          "content": "\"use client\";\n\nimport React, { useState } from 'react';\nimport { cn } from \"@/lib/utils\";\n\nexport interface MorphToggleProps {\n  defaultChecked?: boolean;\n  size?: 'sm' | 'md' | 'lg';\n  label?: string;\n  onChange?: (checked: boolean) => void;\n    className?: string;\n}\n\nexport const MorphToggle = React.forwardRef<any, MorphToggleProps>(({ className, defaultChecked = false, size = 'md', label, onChange, ...props }, ref) => {\n        const [checked, setChecked] = useState(defaultChecked);\n\n        const toggle = () => {\n        const next = !checked;\n        setChecked(next);\n        onChange?.(next);\n        };\n\n        const sizeClass = size === 'md' ? '' : size;\n\n        return (\n        <div ref={ref} {...props} className={cn(\"flex items-center gap-3\", className)}>\n          <button\n            role=\"switch\"\n            aria-checked={checked}\n            onClick={toggle}\n            className={`morph-toggle-track ${checked ? 'on' : 'off'} ${sizeClass}`}\n          >\n            <span className=\"morph-toggle-thumb\" />\n          </button>\n          {label && (\n            <span className=\"text-sm font-medium text-white/70\">{label}</span>\n          )}\n        </div>\n        );\n        });\n"
        }
      ],
      "dependencies": []
    },
    "jsx": {
      "language": "jsx",
      "files": [
        {
          "name": "MorphToggle.jsx",
          "content": "\"use client\";\n\nimport React, { useState } from 'react';\nimport { cn } from \"@/lib/utils\";\n\n  size?: 'sm' | 'md' | 'lg';\n  label?;\n  onChange?: (checked) => void;\n    className?;\n}\n\nexport const MorphToggle = React.forwardRef<any, MorphToggleProps>(({ className, defaultChecked = false, size = 'md', label, onChange, ...props }, ref) => {\n        const [checked, setChecked] = useState(defaultChecked);\n\n        const toggle = () => {\n        const next = !checked;\n        setChecked(next);\n        onChange?.(next);\n        };\n\n        const sizeClass = size === 'md' ? '' : size;\n\n        return (\n        <div ref={ref} {...props} className={cn(\"flex items-center gap-3\", className)}>\n          <button\n            role=\"switch\"\n            aria-checked={checked}\n            onClick={toggle}\n            className={`morph-toggle-track ${checked ? 'on' : 'off'} ${sizeClass}`}\n          >\n            <span className=\"morph-toggle-thumb\" />\n          </button>\n          {label && (\n            <span className=\"text-sm font-medium text-white/70\">{label}</span>\n          )}\n        </div>\n        );\n        });"
        }
      ],
      "dependencies": []
    }
  },
  "preview": {}
}